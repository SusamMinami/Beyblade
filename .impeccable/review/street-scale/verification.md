# 街角生活比例与日夜验证

2026-09-16，Web 当前工作区。浏览器为本机 Chrome，隔离存档；为避免并行文件编辑
触发页面热重载，专项复查使用关闭 HMR 的临时 Vite 服务 `127.0.0.1:5175`。
常规用户入口仍为 `127.0.0.1:5173/#map`。

- `npm test`：29 项通过。
- `npm run build`：通过；保留既有 Three.js 大包警告（约 888 kB）。
- `npm run verify:worlds`：31 项通过。
- `npm run verify:campaign`：65 项通过。
- `node tools/verify-street.mjs`：33 项通过，详细测量见 [verification.json](verification.json)。
- 补充真实 UI 操作：键盘 Home/Enter 选回自动模式、本地时钟解析、切入故事模式
  保留选择、街头剧情战斗以白天启动，均通过。
- `git diff --check`：通过。

渲染测量：默认陀螺宽约 1.99，门高/陀螺宽约 34.42，车轮直径/陀螺宽约 10.94。
盘几何直径仍为 13.4，街头表面、碗力及半径与标准场一致。这些是渲染比例，
不是新增物理单位。

四张战斗图与四张地图图覆盖桌面 564×1000 游戏壳和手机 390×844。
首轮的遮挡与尺寸变化导致选图漂移已修正；本目录 PNG 为最终复查结果。
[独立审阅](review.md) 为 `ship`，仅覆盖这些街头静态图。
通用独立代理代替专用 finish reviewer；动效、暂停、资源释放引用专项运行结果。

共享反射使用一个 768² 目标，手机初次进入使用 512²。本机单帧记录
143 draw calls、876080 submitted triangles（包括反射等重复绘制），
不能据此宣称低端手机性能达标。Godot 与外部多人服务不在此次交付内。
