# 回声远征验收记录

日期：2026-09-16。范围：Web 任务、选场页扩充、陈列室入口与战斗结算。

## 自动化结果

| 检查 | 结果 | 边界 |
| --- | --- | --- |
| `npm test` | 29 通过 | 既有计算、经济、出战槽、模拟回归 |
| `npm run build` | 通过 | 仍有既有 Three.js 大 chunk 提示 |
| `npm run verify:campaign` | 65 通过 | 十关配置与真实模拟结束、进度与去重、锁定预览、浏览器真实首战、刷新恢复、退出和教学入口 |
| `npm run verify:worlds` | 31 通过 | 地图、碰撞、近景操作；入口不再遮挡近景按钮 |
| `npm run verify:lab` | 27 通过 | 实验、商店、经验、报告、导航 |
| `npm run verify:showroom` | 25 通过 | 陈列室升降、预览、解锁、风场 |
| `git diff --check` | 通过 | 未发现补丁空白错误 |

浏览器使用隔离上下文，没有更改用户浏览器存档。主线浏览器首战通过实际
RAF 与固定步长求解器结束，没有注入虚构胜负。其它九关验证真实求解器可以结束，
过关状态用独立状态函数输入验证；这不等于人工玩通、难度平衡或全部结局 UI 验证。

同名日志位于本目录。第一次主线检查发现同文档 hash 导航不生效，已接入
hashchange；第一次世界回归发现新增入口遮挡近景按钮，入口已整合进现有导航。
修改后主线与世界检查完整重跑通过。

## 界面验收

独立审阅结论：**SHIP，限当前六份界面截图**。
该环境没有技能同名 reviewer/documenter 子代理，由独立通用代理承担对应职责，
没有用构建者自己的截图判断替代独立审阅。

- F1 已解决：发射前任务提示使用不透明底板，与发射参数区分开。
- F2 已解决：结算故事正文为 14px 左对齐，奖励与继续按钮保持可见。
- 当前六份截图未发现明显内容溢出或控件互相遮挡。

有效证据：

- `desktop.png`：1440×1000 浏览器中的 564×1000 游戏壳截图。
- `mobile.png`、`locked-mobile.png`：390×844 任务页与锁定预览。
- `launch-mobile.png`、`result-mobile.png`：真实首战的发射和结算。
- `collection-mobile.png`：390×844 浏览器中的 390×694 陈列室游戏壳。

早期根目录 `campaign-desktop.png` / `campaign-mobile.png` 仅为初查。
本目录旧版 `collection-mobile.png` 一度错误捕获了战斗页，已重新捕获真实陈列室，
不得把旧截图当作陈列室证据。

检测器运行一次，返回调色板登记建议；新增 UI 继承已有白纸/黑线/黄蓝配色，
不是重做全局视觉系统的理由。相关约定合并至 Web DESIGN。

## 尚未证明

- 全程手感、难度曲线、耗时与角色对白的情感效果，需人工试玩。
- 独立视觉审阅未覆盖展开后的完整战历、败局、最终通关画面、键盘全流程或动态。
- 无新地图资产、NPC 模型、过场动画、专属技能、自适应 AI 或 Godot 移植。
