# Web 场景进入耗时：诊断与优化

核对：2026-09-17。适用 `web-prototype/`，不包含 Godot。

## 结论

主要问题是首次绘制准备，以及切换页面时重复销毁和重建。
本地测试中，网络传输不是街头、冠军场进入卡顿的主要部分。
第一轮已实现按需加载、当前场景复用、陀螺与缩略图缓存、异步材质准备。
第二轮进一步修正预编译变体，并在地图准备页面预热实际下一场配置；
见本文末尾的第二轮记录。
模型、材质效果、阴影分辨率、日夜灯光及物理配置保持原规格。

## 原因与证据

1. **文件到达不等于画面就绪。** 原实现把 GLB 加入场景后立即报告 ready，
   随后的第一帧才进行着色器编译、几何/纹理上传、阴影与反射绘制。
   街头首进基线中，ready 约 77ms，强制首帧绘制约 4652ms；
   从街头预览进入战斗，ready 约 155ms，首帧约 3013ms。
   首帧计时涵盖多种 CPU/GPU 准备，不能把全部时间都归为着色器编译。
2. **同一场景反复拆建。** `showArena()` 和 `prepareBattle()` 原先都销毁
   当前 GLB、街头积水反射、两套塑料反射探针或冠军灯组，再加载同一文件。
   浏览器文件缓存不能复用这些已释放的场景对象和材质程序。
3. **每次返回都重建陀螺和三张缩略图。** Lab/Showroom 的 refresh 及载入回调
   重复建模、渲染和 resize。普通 CPU 条件返回约 230ms；
   4 倍 CPU 限速后返回达到约 1 秒。
4. **陈列室顺带下载无关房间。** 两个实验室、两个舞台通过构造函数立即加载，
   共 4 个 GLB、7.40 MB。默认陈列室实际上只需要 1 个 GLB、1.76 MB。
   资源计时包含主线程忙碌造成的事件交付延迟，不能直接解释为纯下载耗时。

## 已实现

- 同一地图预览、进入战斗、返回预览复用 GLB、环境反射和灯组。
  每局仍创建新的参赛陀螺，清空损伤模型、碰撞效果、场景脉冲和区域显示状态。
  换另一张地图或进入改装台仍释放旧场景；没有无限增长的地图缓存。
- 陀螺模型按实际零件、涂装和 DIY 值判断是否重建。缩略图共享于实验室与陈列室，
  缓存只保留现有配置，改色/改装会失效；名称、金币与测试结果不触发重复建模。
- 实验室与展示舞台按当前选择加载；同一文件的并发加载合并。
  加载失败可重试，迟到的结果不替换新选择。锁定舞台预览不装备、不改变所有权。
- `prepare-scene.js` 使用 Three.js `compileAsync()` 准备可见材质，
  保留自定义材质代码与实际输出目标。临时材质持有待编译程序，避免用户快速切换、
  释放原材质时异步编译访问已释放对象。完成首帧提交后才报告 ready。
  这并不保证浏览器已完成屏幕合成，也不保证所有阴影、反射变体都能完全异步。
- 实验室只在拥有共享画布时 resize/绘制，避免后台实验室覆盖陈列室画布。

## 测量方法与结果

脚本：[measure-scene-loading.mjs](../web-prototype/tools/measure-scene-loading.mjs)。
隔离存档，依次陈列室→实验室→来回切换→标准地图→冠军/街头/遗迹预览与战斗。
选场后等待已有轮播滚动提交，避免把另一张地图误计为当前地图。

本机 Windows、Chrome 152.0.7977.84 Headless、NVIDIA RTX 4080 / ANGLE D3D11，
1440×1000，DPR 1；支持 `KHR_parallel_shader_compile`。
测量从操作开始到资源准备、一次实际绘制、`gl.finish()` 及两次 RAF，
用来对比可见帧路径，不是线上用户感知延迟的精确测量。
同一脚本分别取改动前和最终工作区的结果，没有在性能采样期间并行运行其他浏览器验收。

| 路径 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 首次进陈列室 | 2303ms | 2063ms |
| 首次进测试室（在陈列室之后） | 1012ms | 475ms |
| 再次进陈列室 | 235ms | 32ms |
| 再次进测试室 | 234ms | 30ms |
| 首次标准地图 | 977ms | 679ms |
| 首次冠军场预览 | 1617ms | 1331ms |
| 冠军场预览→战斗 | 1914ms | 531ms |
| 冠军场战斗→地图 | 127ms | 23ms |
| 首次街头预览 | 4736ms | 3213ms |
| 街头预览→战斗 | 3180ms | 1032ms |
| 街头战斗→地图 | 235ms | 31ms |
| 首次遗迹预览 | 426ms | 256ms |
| 遗迹预览→战斗 | 1501ms | 387ms |
| 遗迹战斗→地图 | 78ms | 29ms |
| 离开其他地图后重新加载街头 | 198ms | 230ms |
| 离开其他地图后重新加载冠军场 | 123ms | 148ms |

完整原始记录：[before.json](../.impeccable/review/loading/before.json)、
[after.json](../.impeccable/review/loading/after.json)。
`cached.json` 是尚未加入异步材质准备的中间实验，不是最终结果。

**边界：** 这些是固定路径的单次采样，不是多次分布或 P95。
首次进入仍受驱动编译缓存与本机负载影响；不能把小幅变化视为稳定收益。
禁用 Chrome 着色器磁盘缓存并不清除所有驱动缓存。
重新加载曾经进入过的另一张地图，在普通 CPU 条件下增加了约 25–32ms
的异步准备调度时间；并非所有路径都更快。

同一浏览器进程内还执行 4 倍 CPU 限速：陈列室返回 1168→161ms、
测试室返回 999→105ms、冠军场预览到战斗 1436→910ms、
街头预览到战斗 1432→845ms。部分首次/换图路径没有收益。
这组运行已经复用较暖的驱动缓存，所以不能直接与上一表的首次进入时间比较。
CPU 限速也不等于低端手机 GPU，尚未做实体移动设备或公网慢网验证。

## 回归与画面核对

- `npm test`：29；`npm run build` 通过，保留既有 Three.js 大 chunk 提示
  （约 890 kB，gzip 267 kB）。
- 实验室 27、陈列室 25、场景/碰撞 31、街头 33、冠军场 24、
  流程 41、教学成长 57 项检查通过。
- 新增 `npm run verify:loading`：17 项，包括按需请求、缓存失效、锁定预览重试、
  共享画布归属、同场景复用、每局重置、迟到响应、编译中销毁和无未捕获异常。
- 街头、冠军场检查调整为等待异步准备结束后再统计绘制与资源，
  避免把未绘制的场景当作内存基线。反射、灯光像素变化、暂停、减少动态效果、
  日夜切换与释放后几何/纹理计数回归通过。
- 桌面及手机宽度截图与先前已提交画面对照，未发现模型、材质或布局缺失。
  动态灯光和陀螺角度的不同属于采样时刻差异；未宣称逐像素完全相同。

证据集中在 [loading/](../.impeccable/review/loading/)；
[桌面对比](../.impeccable/review/loading/comparison-desktop.png)、
[手机对比](../.impeccable/review/loading/comparison.png)、
[生命周期检查](../.impeccable/review/loading/verification.json)。
既有场景评审目录保留历史版本，没有用本轮自动截图覆盖旧交付记录。

复现：先启动本地 dev server，然后在 `web-prototype/` 执行
`npm run measure:loading -- my-run` 和 `npm run verify:loading`。
两者支持 `CHROME_PATH`、`LAB_TEST_URL`；均使用隔离浏览器存档。

## 第二轮：下一场配置与首次 GPU 准备

基线为第一轮提交 `d5687d6`，不是本文第一轮表格里的最早版本。
第二轮修改继续仅适用于 Web 原型。

### 新定位的原因

- 材质副本的 `clone()` 会重建 Physical 材质的 `defines`。部分导入表面沿用
  Standard 定义，预编译副本却变成另一套程序；实际首次绘制又编译原来的变体。
  本轮精确保留源材质定义、自定义 shader 和纹理引用，保持原有画面。
- Three r178 的 `compileAsync()` 不初始化逐材质的裁切状态。
  陈列室升降裁切的一平面变体在首次绘制才被创建。普通地图的实例化对象、
  双面透明物体前后两面也需要匹配实际对象类型与渲染状态。
- 在新增的固定路径诊断中，街头预览第一次 render 约 **1822ms**，
  包含约 **1192ms** 的积水反射嵌套 render。几何 `bufferData` 调用合计约
  **2.8ms**。这些是 JS 调用计时，不能解释为 GPU 执行总耗时；
  证据支持优先解决重复着色器准备，而不是降低模型精度。

### 本轮实现

- `prepare-scene.js` 精确保留材质定义、实例属性、透明双面与裁切数量；
  按批提交异步编译，然后以不写入可见像素的绘制准备真实几何/纹理。
  批次以 4ms 为让出主线程的目标；单个 Three/驱动调用无法中途打断，
  这不是每帧耗时的硬上限。真实场景仍完整绘制一次才报告 ready。
- 地图 ready 后等待 600ms，再利用浏览器空闲回调准备**当前玩家与实际对手**
  的两只陀螺和发射器。自由对战、教学和剧情共用参赛配置解析函数。
  陀螺按原有五个零件分步创建，几何生成算法、DIY、物理均未改变。
- 一次只保留一组预备配置；快速切换时取消旧任务，新任务等待旧编译安全结束。
  改色、DIY、地图、对手、日夜不同都会失效。离开准备页面或销毁时取消，
  锁定剧情关卡不预热，不支持并行着色器编译的设备跳过可选预热。
- 编译好的程序由临时材质持有到开战首帧，再释放引用。匹配时直接接用
  预备模型；已开过一局的受损模型不会作为下一局的预备模型。
  未完成预热就点击开战，立即采用正常加载流程，不等待背景任务。
- 后台准备不推进剧情、不修改所有权、存档或选择；不提前显示陀螺，
  不改动场景灯光、阴影分辨率与反射效果。

### 第二轮测量

`npm run measure:gpu -- <记录名>` 为隔离浏览器增加 shader、render、上传与
长任务探针；操作后等待 ready、`gl.finish()` 及两次 RAF。
Chrome 152 / RTX 4080、1440×1000、DPR 1，与前述硬件一致。
性能测量期间没有并行执行其他浏览器验收。地图预览与开战间固定停留
**2.5 秒**模拟阅读，停留期间的成本单独记录，不算进开战时间。

| 路径 | 第二轮前 | 第二轮后 |
| --- | ---: | ---: |
| 首进陈列室 | 1424ms | 914ms |
| 首进测试室（陈列室后） | 279ms | 294ms |
| 返回陈列室 | 28ms | 34ms |
| 首进标准地图 | 580ms | 260ms |
| 首进冠军场 | 1157ms | 548ms |
| 冠军场阅读后开战 | 500ms | 85ms |
| 首进街头 | 2848ms | 1089ms |
| 街头阅读后开战 | 875ms | 56ms |

原始证据：[第二轮前](../.impeccable/review/gpu-preparation/before.json)、
[最终结果](../.impeccable/review/gpu-preparation/final.json)。
本轮仍为固定路径单次采样，不能称为 P95 或实体手机结果。
第一轮到第二轮的测量时刻、驱动缓存不同，不跨表混算百分比。

普通 CPU 采样的阅读预热期间没有超过 50ms 的长任务。4 倍 CPU 限速下，
阅读后冠军场开战 **393→168ms**、街头 **359→107ms**；
分步建模后仍出现 **54ms、62ms** 长任务。已较暖的驱动缓存下，
冠军场首进 **344→564ms**、街头首进 **487→619ms**，分批调度有开销。
本轮不宣称所有设备、所有路径均加速。

另用原 `measure:loading` 的 400ms 短停留路径测试未预热完成的情况：
冠军开战 609ms、街头 893ms，返回陈列室 29ms、测试室 24ms。
与第一轮最终采样的 531ms、1032ms 相比，冠军这一路略慢；
预热收益依赖玩家的实际停留时间。原始数据：
[短停留结果](../.impeccable/review/loading/gpu-final.json)。

### 第二轮验证

- `npm test` 29 项、`npm run build` 通过；保留既有 Three.js 890 kB
  （gzip 267 kB）chunk 提示。
- 新增 GPU 预热 21、加载生命周期 17、实验室 27、陈列室 25、场景/碰撞 31、
  冠军场 24、流程 41、教学成长 57、街头 33 项浏览器检查通过。
- 预热采用、未完成时开战、锁定关卡、改色、日夜切换、编译中离开、销毁、
  无并行编译扩展及共享画布归属均有检查。重复预热后取消三次，
  街头基线均为 60 个 geometry、22 个 texture，没有累计增长。
  这验证了 Three.js 资源计数，不等同于精确测得驱动显存。
- 桌面与手机宽度对照未见陈列室升降裁切、童年实验室、冠军金属、
  街头积水反射或布局缺失。灯光、模型角度因采样时刻稍有不同。
  本轮不新增美术调整，也没有实体移动设备验收。

证据：[GPU 生命周期](../.impeccable/review/gpu-preparation/verification.json)、
[桌面对照](../.impeccable/review/gpu-preparation/comparison-desktop.png)、
[手机对照](../.impeccable/review/gpu-preparation/comparison-mobile.png)、
[街头对照](../.impeccable/review/gpu-preparation/comparison-street.png)。
本轮最终场景对照、测量与验证日志归档到
[第二轮证据目录](../.impeccable/review/gpu-preparation/)；已还原历史评审目录，
不覆盖原来的交付证据，也不保留调试阶段的中间采样。

## 后续机会（尚未实现）

1. 缩短首次陈列室的同步构造与缩略图准备；当前仍约 0.9 秒。
   下一场预热已实现，但实体低端设备的建模预算仍需实测。
2. 继续分析驱动缓存较暖时的分批调度开销，尤其是 4 倍 CPU 限速下的换图。
   不能为了隐藏性能指标而将大段等待挪到地图操作上。
3. 公网版本测量 GLB 压缩、缓存头和资源分发；验证视觉无损的几何压缩方案。
   压缩可减少下载，但不能替代着色器和重复建模优化。
4. 在实体手机测量首次进入、显存与持续帧率，再决定是否需要可选择的画质档。
   本次没有通过减少灯光、反射、模型细节或改变物理来换取更快进入。
